import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  footer: {
    width: '100%',
    height: 50,
    padding: 16,
    alignItems: 'center',
    backgroundColor: '#000',
  },
  footerTitle: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#fff',
  },
});

export default styles;
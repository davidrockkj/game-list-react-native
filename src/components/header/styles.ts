import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    height: 150,
    width: '100%',
    padding: 16,
    alignItems: 'center',
    backgroundColor: '#000',
  },
  headerTitle: {
    textAlign: 'center',
    fontSize: 24,
    fontWeight: 'bold',
    color: '#fff',
  },
  input: {
    width: '80%',
    padding: 8,
    marginTop: 12,
    borderRadius: 4,
    backgroundColor: '#fff',
  },
});

export default styles;